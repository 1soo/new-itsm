# API 베이스 URL — dev proxy(same-origin) 경로. apiClient는 미설정 시 "/api/v1"로 동작.
VITE_API_BASE_URL=/api/v1

# dev 서버 프록시(/api) 대상 백엔드 주소. 미설정 시 "http://localhost:8080"로 동작.
VITE_PROXY_TARGET=http://localhost:8080
