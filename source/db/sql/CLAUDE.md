# CLAUDE.md

도메인별 스키마 DDL과 시드 데이터. 컨테이너 최초 기동 시 파일명 알파벳(번호) 순 실행. 단일 원천은 `docs/02_plan/database/*.md`이며 공통 컬럼 규칙(id, created_by/at, updated_by/at, is_deleted)을 따름.

## 파일
- `01_schema.sql` — auth 도메인 스키마: `role`, `app_user`, `user_role`, `screen`, `screen_role`(RBAC), `refresh_token`, `audit_log`(뒤 둘은 append-only).
- `02_seed.sql` — auth 시드: 역할 11종, SYSTEM_ADMIN 초기 계정(admin@itsm.local), auth/admin/common/error 화면(SCR-*)과 역할-화면 매핑.
- `03_common_schema.sql` — 티켓 공용 다형 참조 테이블: `approval`(승인), `comment`(코멘트), `timeline_event`(타임라인). ticket_type/ticket_id 다형 참조라 FK 대신 앱에서 검증.
- `04_srm_schema.sql` — service-request 스키마: `queue`, `service_catalog_item`, `catalog_form_field`, `service_request`, `service_request_form_value`(EAV), `csat`(만족도).
- `05_srm_seed.sql` — service-request 시드: 기본 큐 2종, 예시 카탈로그 항목·동적 양식 필드, 화면(SCR-SRM-001~008)과 역할-화면 매핑 증분.
- `06_incident_schema.sql` — incident 스키마 + 공용 `ticket_link`(다형 링크) 도입: `incident`, `incident_responder`, `incident_severity_history`, `postmortem`, `postmortem_five_why`, `postmortem_action_item`.
- `07_incident_seed.sql` — incident 시드: 화면(SCR-INC-001~005)·역할-화면 매핑 증분, 테스트 계정(im@/agent@itsm.local, INCIDENT_MANAGER/SERVICE_DESK_AGENT). 트랜잭션 데이터는 seed 안 함.
- `08_problem_schema.sql` — problem 스키마: `problem`(문제 티켓), `problem_five_why`(RCA), `known_error`(KEDB), `problem_action`(후속 조치).
- `09_problem_seed.sql` — problem 시드: 화면(SCR-PRB-001~004)·역할-화면 매핑 증분, 테스트 계정(pm@itsm.local, PROBLEM_MANAGER). 트랜잭션 데이터는 seed 안 함.
- `10_change_schema.sql` — change 스키마: `change_template`(표준 변경 사전승인 템플릿), `change_request`(RFC, 6단계 상태·승인경로·구현결과), `change_affected_system`(영향 시스템). 승인·연계는 `approval`/`ticket_link` 재사용.
- `11_change_seed.sql` — change 시드: 표준 변경 템플릿 1건, 화면(SCR-CHG-001~006)·역할-화면 매핑 증분(CHANGE_MANAGER/APPROVER, 둘 다 auth 단계 기존 역할 재사용), 테스트 계정(cm@/cab@itsm.local). 트랜잭션 데이터는 seed 안 함.
- `12_knowledge_schema.sql` — knowledge 스키마: `knowledge_category`/`knowledge_label`(분류), `knowledge_article`(기사, DRAFT/IN_REVIEW/PUBLISHED, 유용성·조회 집계), `article_label`(매핑), `knowledge_feedback`(유용성 평가), `knowledge_review`(검토 이력), `search_log`(무결과 검색 로그, append-only). KCS 연계는 `ticket_link` 재사용.
- `13_knowledge_seed.sql` — knowledge 시드: 카테고리 3건(네트워크/계정·권한/결제), 화면(SCR-KM-001~005)·역할-화면 매핑 증분(KNOWLEDGE_CONTRIBUTOR/KNOWLEDGE_GATEKEEPER, 둘 다 auth 단계 기존 역할 재사용), 테스트 계정(kc@/kg@itsm.local). 트랜잭션 데이터는 seed 안 함.
- `14_asset_schema.sql` — asset 스키마(7/7 마지막 도메인): `asset`(asset_key AST-####, 생애주기 5단계, 만료일 3종), `asset_attribute`(EAV), `asset_lifecycle_history`(append-only), `expiry_alert`, `configuration_item`(CI), `ci_relation`(CI 자기참조 의존관계, 자기참조 금지 CHECK). 티켓 연계는 `ticket_link`(source_type='ASSET'/'CI') 재사용.
- `15_asset_seed.sql` — asset 시드: 화면(SCR-ITAM-001~005)·역할-화면 매핑 증분(ASSET_MANAGER, auth 단계 기존 역할 재사용), 테스트 계정(am@itsm.local). 트랜잭션 데이터(asset 등)는 seed 안 함.
- `16_esm_schema.sql` — esm(엔터프라이즈 서비스 관리, 확장 도메인 1/4) 스키마: auth 증분 `app_user.department` 컬럼 추가(NULL 허용, HR/LEGAL/FACILITIES/FINANCE/IT) + 신규 테이블 8개(`esm_catalog_item`, `esm_catalog_form_field`(EAV), `esm_checklist_template_task`, `esm_checklist`, `esm_checklist_task`(자산 회수는 `asset.id` FK, nullable), `esm_request`, `esm_request_form_value`(EAV), `esm_hr_case`(민감정보)). 코멘트/타임라인은 `common`(03) 재사용.
- `17_esm_seed.sql` — esm 시드: 신규 역할 2종(`HR_CASE_MANAGER`, `DEPT_COORDINATOR`) + 해당 역할의 공통 기본 접근(02_seed CROSS JOIN 시점에 없던 역할이라 별도 증분), 화면(SCR-ESM-001~011)·역할-화면 매핑, 테스트 계정(hr@/legal-coord@/facilities-coord@/it-coord@itsm.local — 서로 다른 부서로 department 접근 제어 테스트용, it-coord는 오프보딩 자산회수 하위작업 완료·체크리스트 전체 자동완료 시나리오 검증용), 부서별(HR/LEGAL/FACILITIES/FINANCE) 카탈로그 항목 5건(온보딩·오프보딩 각 1건 + 체크리스트 템플릿 하위 작업 각 2건, 서로 다른 부서 배정).
- `18_vulnerability_schema.sql` — vulnerability(취약점 관리, 확장 도메인 2/4) 스키마: common 증분 `ticket_link` CHECK(source_type/target_type)에 `VULNERABILITY` 추가 + 신규 테이블 3개(`vulnerability`(ticket_key VULN-YYYY-####, severity×exploitability→risk_score 캐시(1~12, BE 산식 가중치 4/3/2/1 곱의 실제 상한에 맞춤), 6단계 상태), `vulnerability_remediation`(개선 조치 이력), `vulnerability_verification`(검증 결과 이력, 재검증 반복)). 코멘트/타임라인은 `common`(03) 재사용(ticket_type에 CHECK 없어 증분 불필요).
- `19_vulnerability_seed.sql` — vulnerability 시드: 신규 역할(`VULNERABILITY_MANAGER`, 단일) + 공통 기본 접근 증분(02_seed CROSS JOIN 시점에 없던 역할), 화면(SCR-VULN-001~004)·역할-화면 매핑, 테스트 계정(vm@itsm.local), 심각도별(CRITICAL/HIGH/MEDIUM/LOW) 예시 취약점 4건(그중 CRITICAL 1건은 자산이 존재하면 `ticket_link`로 연계, 자산이 없으면 스킵).
- `20_compliance_schema.sql` — compliance(컴플라이언스 관리, 확장 도메인 3/4) 스키마: common 증분 `ticket_link.source_type` CHECK에 `COMPLIANCE_REQUIREMENT` 추가(값 22자가 기존 VARCHAR(20)을 초과해 컬럼 폭도 VARCHAR(25)로 확장, target_type은 기존 'CHANGE' 값 재사용이라 변경 없음) + auth 증분 `audit_log.event_type` 폭 확장(VARCHAR(30)→(35), 신규 이벤트타입 `COMPLIANCE_ACTION_STATUS_CHANGE` 31자 초과분 대응) + 신규 테이블 2개(`compliance_requirement`(requirement_key COMP-YYYY-####, owner_id nullable), `corrective_action`(DETECTED/IN_PROGRESS/RESOLVED, (requirement_id, status) 인덱스로 미해결 건수 집계)). 준수 상태(compliance_status)는 저장 컬럼 없이 조회 시점 계산값. 감사 로그는 auth.audit_log(19) 재사용.
- `21_compliance_seed.sql` — compliance 시드: 신규 역할(`COMPLIANCE_OFFICER`, 단일) + 공통 기본 접근 증분(02_seed CROSS JOIN 시점에 없던 역할), 화면(SCR-COMP-001~004)·역할-화면 매핑, 테스트 계정(co@itsm.local), 예시 요구사항 3건(책임자 지정 2건/미지정 1건, 그중 1건은 미해결 시정조치로 NON_COMPLIANT, 1건은 RESOLVED 시정조치 보유하지만 COMPLIANT), 요구사항 1건은 change_request가 존재하면 `ticket_link`로 연계(없으면 스킵).
- `22_infra_monitoring_schema.sql` — infra-monitoring(IT 인프라 모니터링 & 용량관리, 확장 도메인 4/4 마지막) 스키마: 신규 테이블 5개(`infra_metric`(asset_id FK, metric_type UPTIME/CPU/MEMORY/RESPONSE_TIME, 시계열 append), `infra_metric_threshold`(metric_type UNIQUE 전역 임계치), `infra_metric_alert`(metric_id/asset_id FK, asset_id·metric_type 비정규화, threshold_type UPPER/LOWER), `uptime_target`(asset_id FK UNIQUE, SLA 목표), `capacity_plan`(team_or_service, capacity, demand)). 가동률·용량 활용률은 저장 컬럼 없이 조회 시점 계산값. (asset_id, metric_type, measured_at) 조회 인덱스. common/auth 테이블 증분 없음(비-티켓형 도메인).
- `23_infra_monitoring_seed.sql` — infra-monitoring 시드: 신규 역할(`INFRA_OPERATOR`, 단일) + 공통 기본 접근 증분(02_seed CROSS JOIN 시점에 없던 역할), 화면(SCR-IOM-001~005)·역할-화면 매핑, 테스트 계정(io@itsm.local), 임계치 2건(CPU 상한 90/RESPONSE_TIME 상한 500) 사전 설정, 기존 자산 중 존재하는 만큼(최대 3건, 순번 기준)에 지표 8건 시드(그중 CPU 92.30/RESPONSE_TIME 550.00은 임계치 초과 케이스라 대응하는 `infra_metric_alert` 2건도 함께 시드), 자산 1건 가동률 목표(uptime_target), 용량 계획 3건(활용률 120% 초과 케이스 1건 포함). 자산이 없는 환경에서는 지표/알림/가동률목표 시드가 자동으로 스킵됨(에러 아님).
- `24_auth_menu_columns.sql` — auth 증분(Role-Menu 동적 매핑, 유지보수 요청 2026-07-11): `screen`에 사이드바 표시 컬럼 5종(`icon_name`/`group_code`/`group_label`/`sort_order`/`nav_visible`) 추가 + `path` UNIQUE 제약(`uq_screen_path`). 과거 라우트 변경이 반영 안 됐던 `screen.path` 7건(SCR-KM-004/SCR-ITAM-004/SCR-IOM-001~005) 보정. 그동안 시드된 적 없던 SCR-COM-010(테마 토글)/011(통합 검색 결과)/012(사용자 가이드) 신규 시드(비-라우트/헤더 전용, `nav_visible=false`) + SCR-COM-013(대시보드 "/", 설계 당시 코드 미부여로 누락됐던 전 인증 사용자 공통 홈, `nav_visible=true` 최상단 단독 항목) + SCR-ADMIN-006(메뉴 관리, 신규 관리자 화면·`screen_role`에 SYSTEM_ADMIN 매핑) 추가. 단일 원천 `source/frontend/src/routes/navConfig.tsx` 기준으로 전 화면의 사이드바 표시 컬럼 백필(그룹·아이콘·정렬순서, navConfig 미등재 화면은 `nav_visible=false`). `screen_role`은 그대로 재사용(신규 테이블 없음).
- `25_common_notification_dismissal.sql` — common 증분(알림 확인처리, 유지보수 요청 2026-07-11): 신규 테이블 `notification_dismissal`(사용자별 헤더 알림 "모두 지우기"·개별 X 확인처리 이력, append-only이라 `updated_*`/`is_deleted` 미사용). `UNIQUE(user_id, notification_type, source_id)`로 중복 확인처리 방지(API-COM-001 멱등 처리 근거) + 조회 인덱스(API-COM-002 이력 조회). 시드 데이터 없음(사용자 조작으로만 채워짐).
- `26_approval_engine_schema.sql` — 공용 승인 프로세스 엔진 도입(유지보수 요청 2026-07-11): 신규 테이블 8개(`approval_process`(규칙 헤더, tier 1/2 부분 유니크 인덱스)/`approval_process_requester_role`/`approval_process_step`(1~10차, decision_mode AND/OR)/`approval_process_step_role`/`approval_request`(인스턴스 헤더, 다형 참조 ticket_type+ticket_id)/`approval_request_step`(스냅샷)/`approval_request_step_role`(스냅샷)/`approval_decision`(append-only, updated_*/is_deleted 미사용)) + 기존 `approval` 테이블 제거(03번 원본 미수정, 이 파일에서 DROP) + `service_catalog_item.approval_required`/`approver_role`, `change_request.approval_route` 컬럼 제거(04/10번 원본 미수정). 신규 role 없음(기존 role 재사용).
- `27_approval_engine_seed.sql` — 공용 승인 프로세스 엔진 시드(유지보수 요청 2026-07-11): 기존 `SCR-SRM-006`(승인 대기함)·`SCR-CHG-004`(CAB 승인 대기함) screen/screen_role 삭제(공유 approval 테이블 제거로 구 도메인별 승인 화면 폐기, `SCR-COM-014`로 완전 대체) + 신규 screen 3개 시드: `SCR-ADMIN-007`(승인 프로세스 목록, `/admin/approval-processes`, SYSTEM_ADMIN 전용)·`SCR-ADMIN-008`(승인 프로세스 생성/편집, `/admin/approval-processes/new`, nav_visible=false 서브 화면, SYSTEM_ADMIN 전용)·`SCR-COM-014`(승인 대기함 전 도메인 공용, `/approvals`, screen_role 매핑 없음=전체 인증 사용자 공개). 경로·아이콘·그룹·정렬은 developer-fe-2 확정치 반영. 신규 role 없음, `approval_process*` 트랜잭션 데이터는 seed 안 함(SYSTEM_ADMIN이 SCR-ADMIN-008에서 직접 구성).
- `28_approval_engine_index_fix.sql` — 버그 수정(tester 발견, 2026-07-11): `26_approval_engine_schema.sql`의 tier=1/2 부분 유니크 인덱스 2개가 `is_deleted`를 고려하지 않아 soft-delete 후 동일 스코프 재생성 시 500(DataIntegrityViolationException) 발생. 두 인덱스를 DROP 후 WHERE 절에 `is_deleted = false` 조건을 추가해 재생성.
- `29_knowledge_km004_cleanup.sql` — Stage 3(KNOWLEDGE) 정리(2026-07-11): `SCR-KM-004`(검토·게시 승인함) screen/screen_role hard DELETE(FE 페이지·라우트 이미 삭제, `SCR-COM-014`로 완전 대체). Stage 1의 SCR-SRM-006/SCR-CHG-004 정리와 동일 패턴.
- `30_auth_screen_i18n.sql` — auth 증분(사이드바 메뉴 i18n 결함 수정, 유지보수 요청 2026-07-13): `screen`에 `screen_name_en`(영문 메뉴명, NOT NULL)/`group_label_en`(영문 그룹명, group_code 있는 행만) 컬럼 추가. 현재 시점 전체 screen_code(삭제된 SCR-SRM-006/SCR-CHG-004/SCR-KM-004 제외)에 영문 번역값 백필 후 NOT NULL 제약 적용. 백필 누락 시(신규 screen_code 추가하고 이 파일 갱신을 잊은 경우) `DO` 블록에서 예외 발생시켜 마이그레이션 중단.
- `31_sidebar_menu_label_cleanup.sql` — auth 증분(사이드바 메뉴 라벨 괄호 제거, 유지보수 요청 2026-07-14): 사이드바 노출(nav_visible=true) 화면 중 괄호 부연설명이 있던 4건(SCR-SRM-004/SCR-CHG-005/SCR-ESM-001/SCR-COM-014)의 `screen_name`/`screen_name_en`에서 괄호와 괄호 안 내용 제거.
- `32_approval_process_priority_redesign.sql` — common 증분(승인 프로세스 범위 우선순위 재설계, 유지보수 요청 2026-07-15): `approval_process.domain` NOT NULL → NULL 허용(미지정=전체 도메인 적용). `priority_tier` 산정식을 3축(도메인/요청유형/역할) 독립 스코프 기반(축개수×10 + 역할4 + 요청유형2 + 도메인1, 실사용값 0/11/14/23/25/37)으로 재정의하고 기존 행 재계산(백필). 26/28번의 tier=1/2 부분 유니크 인덱스(`uq_approval_process_domain_tier1`/`_subtype_tier2`)를 신규 tier값 기준(`uq_approval_process_tier0`/`_domain_tier11`/`_domain_subtype_tier23`, 모두 `is_deleted=false` 조건 유지)으로 재작성. tier=14/25/37(역할 조합 포함)은 DB 제약 불가라 애플리케이션 검증.
- `33_srm_catalog_assignee_role.sql` — service-request 증분(요청 유형별 담당자 역할 지정, 유지보수 요청 2026-07-15): `service_catalog_item.assignee_role_id`(FK role.id, NULL 허용) 추가. 지정 시 상담원이 라우팅/배정 시점에 해당 역할 보유자 후보 목록에서 담당자를 수동 선택(자동배정 아님), 미지정이면 기존과 동일하게 본인 배정만 가능. 시드: '노트북 신청' 1건에 `SERVICE_DESK_AGENT`로 값 채움(tester의 후보 목록 조회 검증용), 나머지는 NULL 유지.
- `34_srm_catalog_category.sql` — service-request 증분(카탈로그 카테고리 CRUD, 유지보수 요청 2026-07-16): 신규 테이블 `service_catalog_category`(name UNIQUE/sort_order/공통 컬럼) + `service_catalog_item.category_id`(FK) 추가. 기존 `category` 자유 텍스트의 DISTINCT 값을 카테고리로 백필 후 `category_id` 매핑(값 없으면 스킵), 백필 완료 후 `category` 컬럼 DROP(원본 04번 미수정). 시드: '소프트웨어' 예시 카테고리 1건 추가(05_srm_seed 데이터 기준 백필로 이미 생성된 '하드웨어'/'계정'과 합쳐 총 3건, '소프트웨어'는 매핑 항목 없는 후보로 tester 검증용).
- `35_catalog_form_field_textarea_type.sql` — service-request/esm 증분(동적 양식 필드 유형 확장, 유지보수 요청 2026-07-16): `catalog_form_field`/`esm_catalog_form_field`의 `field_type` CHECK 제약(`ck_catalog_form_field_type`/`ck_esm_catalog_form_field_type`)에 `textarea` 값 추가(DROP CONSTRAINT 후 재생성).
- `36_srm_form_schema_jsonb.sql` — service-request 증분(서비스 카탈로그 커스텀 폼 빌더 form.io 전환, 유지보수 요청 2026-07-17): `service_catalog_item.form_schema`(JSONB, Form.io Form JSON `{display,components}`) 신규 + `service_request.form_values`(JSONB, `submission.data` 그대로) 신규. 기존 `catalog_form_field`(EAV) 행을 `sort_order` 순 Form.io 컴포넌트 배열로 변환 백필(`field_type`→`type`: text→textfield/textarea→textarea/select→select(`options`→`data.values[].{label,value}`)/number→number/date→datetime(`enableTime:false`)/file→file(`storage:'base64'`), `required`→`validate.required`) 후 `catalog_form_field` DROP. 기존 `service_request_form_value`(EAV) 행을 `service_request_id`별 `{field_key:field_value}` 객체로 조립 백필 후 `service_request_form_value` DROP(원본 04번 미수정, 26/33/34번 선례와 동일 패턴).
- `37_srm_queue_retirement.sql` — service-request 증분(요청 큐 폐지 → 카테고리 기반 분류 일원화, 유지보수 요청 2026-07-18): `service_catalog_item.queue_id`/`service_request.queue_id` 컬럼 제거 + `queue` 테이블 DROP(스냅샷 컬럼 아니라 백필 불필요). `screen.screen_name`/`screen_name_en`(SCR-SRM-004)을 '요청 처리함'/'Request Inbox'로 갱신(경로·아이콘·그룹·정렬순서는 유지, 원본 04번 미수정).
