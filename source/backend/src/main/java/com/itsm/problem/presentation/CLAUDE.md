# CLAUDE.md

problem 도메인 REST 컨트롤러.

## 파일
- `ProblemController.java` — `/api/v1/problems` 문제 CRUD·상태전이·RCA·워크어라운드·후속조치·연계·종료 API
- `KnownErrorController.java` — `/api/v1/known-errors` 기지 오류 생성·검색 API
