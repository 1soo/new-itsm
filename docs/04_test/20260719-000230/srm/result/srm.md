---
date: 20260719-000230
domain: srm
result: pass
keywords: [미니팝업위치통일, Popover에서Modal전환, top-42퍼센트, 라벨Select최상단이동, dialog미변경회귀없음]
---

# 통합 테스트 결과 — srm 그리드 폼 빌더 7차: 미니팝업 위치·순서 보정 (20260719-000230)

## 요약
- 총 7건 · 성공 7 · 실패 0

## 상세

| TC ID | 결과 | 실제 동작 | 비고(스크린샷/로그) |
|-------|------|-----------|----------------------|
| TC-GF7-B01 | PASS | FE `npm run build`(tsc -b && vite build) 성공(API/DB 변경 없어 BE 빌드 생략, 계획서 명시) | - |
| TC-GF7-001 | PASS | 신규 카탈로그 항목 "GF7 미니팝업 위치 순서 보정 테스트" 생성, text 컴포넌트 Content 설정 팝업을 연 결과 "라벨(태그)" Select가 팝업 최상단에 위치하고, 그 아래로 "input 폭(%)"→"input 정렬"→"Placeholder"→"기본값"→"읽기 전용/필수 여부"→"Validation 정규식" 순으로 이어짐(순서 정확히 일치) | - |
| TC-GF7-002 | PASS | 캔버스 좌상단(text, col0/row0)과 원거리(date, col6/row6, 드래그로 배치) 두 카드에서 각각 Content 설정 팝업을 열어 DOM `getBoundingClientRect()`로 정밀 측정: 두 팝업 모두 가로 x=558(뷰포트 1536 기준 정중앙), 세로 중심점 centerY=323.2(뷰포트 770의 42%)로 픽셀 단위까지 완전히 동일 — 트리거 위치 무관 고정 좌표 확인 | - |
| TC-GF7-003 | PASS | 라벨 생성 팝업("라벨 추가")의 위치를 동일 방식으로 측정한 결과 centerX=768(=1536/2), centerY=323.2 — TC-GF7-002의 Content 설정 팝업과 좌표 중심점이 정확히 일치(폭이 달라(420px vs 512px) 좌측 x값 자체는 다르지만 화면 기준 중심점은 동일, `MINI_POPUP_POSITION_CLASS` 공유 확인) | - |
| TC-GF7-004 | PASS | Content 설정 팝업 내부에서 라벨(태그) Select 지정("중요")·Placeholder 입력("여기에입력")을 조작하는 동안 캔버스 카드 위치(`gridArea`)가 전혀 변하지 않음(드래그로 오인되지 않음). 팝업을 닫자 캔버스 카드에 placeholder와 legend 스타일 라벨 텍스트가 즉시 반영됨. 라벨 생성 팝업 내부 조작(텍스트 입력, "테두리 없음" 체크)도 캔버스에 영향 없음, 저장 후 칩 스트립에 정상 반영. 팝업 열기/닫기(설정 아이콘 클릭·Escape·닫기 버튼) 모두 정상 동작 | shots/gf7_004_reflected_canvas.png |
| TC-GF7-005 | PASS | SRM 카탈로그 카테고리 관리 탭 "새 카테고리" 모달을 열어 측정한 결과 centerX=768, centerY=384.8(뷰포트 770의 정확히 50%, 기존 `top-1/2` 그대로) — SRM 미니팝업(42%)과 다른 정중앙 위치를 유지해 공용 `ui/dialog.tsx` 미변경에 따른 회귀 없음 확인 | shots/gf7_005_category_modal_center.png |
| TC-GF7-R01 | PASS | 캔버스 카드=실제 렌더링 유지. DnD 드래그 직후 즉시 클릭 시 정상 추가 재확인(체크박스 드래그 배치 후 곧바로 클릭해 2번째 체크박스도 정상 추가, count 3→5). 팔레트 9종(label 없음) 유지. 체크박스 Content 설정에 정규식 UI 없음(text 전용 유지)·옵션 배치 방향/여백 정상 노출. 9방향 정렬 위젯(`left-top` 등) 정상. 라벨 legend 스타일(테두리 위에 텍스트 걸침) 재확인 | - |

## 테스트 데이터 처리
- 신규 카탈로그 항목 "GF7 미니팝업 위치 순서 보정 테스트" 생성 및 저장(text 1개+date 1개 컴포넌트, 라벨 "중요"(text에 지정) 1개). 회귀 확인 중 임시로 추가한 checkbox 2개는 "취소" 버튼으로 미저장 종료해 저장 데이터에는 반영되지 않음.

## 실패 항목 분석
없음.
