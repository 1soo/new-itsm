# 통합 테스트 시나리오 — srm (그리드 폼 빌더 4차 재테스트: TC-GF4-B01e 결함 수정 확인)

## 사전 조건
- 이번 재테스트는 직전 실행(`docs/04_test/20260718-210534`)에서 FAIL한 TC-GF4-B01e 단일 건만 재검증한다(dev-lead 지시, 나머지 18건은 이번 수정과 무관해 재확인 불필요).
- Process Owner 권한 계정(`po@itsm.local` / `Admin@1234`, SCR-SRM-007 Form 설정)
- 신규 카탈로그 항목을 생성해 격리된 캔버스에서 검증한다.
- playwright는 새 창(새 browser context)에서 수행, storage 초기화

## 시나리오

### TC-GF4-B01e-R · 드래그 배치 직후 동일 유형 클릭 배치 정상 동작(재검증)
- 근거: 직전 실행 `docs/04_test/20260718-210534/srm/result/srm.md`(TC-GF4-B01e FAIL), dev-lead 수정 보고(`JUST_DRAGGED_RESET_MS` 300→0)
- 절차:
  1. 팔레트 임의 유형(예: 체크박스)을 드래그해 캔버스에 배치
  2. 드래그 종료 직후(지연 없이, 0ms) 같은 팔레트 버튼을 클릭 → 2번째 컴포넌트가 정상 추가되는지 확인
  3. 별도로 다른 유형(예: 날짜)을 드래그 배치 후 300ms 대기 후 클릭 → 정상 추가되는지 확인
  4. 드래그 중 캔버스 밖으로 나갔다가 다시 원래 팔레트 버튼 위로 돌아와 드롭(드래그 취소)한 뒤, 곧바로 같은 버튼을 클릭 → 정상 추가되는지 확인(드래그 취소와 클릭이 뒤섞이지 않는지)
- 기대 결과: 세 시나리오 모두 컴포넌트가 정상적으로 추가됨(클릭이 씹히지 않음)
