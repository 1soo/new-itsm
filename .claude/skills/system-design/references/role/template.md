# 역할 정의 — {역할명}

> 역할: {role} · 버전: 0.1 · 작성일: {YYYY-MM-DD}

## 1. 페르소나

- **역할명**: {role}
- **설명**: {이 역할이 누구이고 무엇을 하는가}
- **주요 목표**: {이 역할이 시스템에서 달성하려는 것}

## 2. 접근 가능 화면

| 화면 ID | 화면명 | 비고 |
|---------|--------|------|
| SCR-... | {화면명} | |

## 3. 접근 가능 API

| API ID | Endpoint | Method | 비고 |
|--------|----------|--------|------|
| API-... | /api/... | GET/POST/... | |

## 4. 접근 제어 규칙

- 로그인 필요 화면/요청 접근 시 Access Token의 `role`을 검증한다.
- 이 역할에 없는 화면/요청 → **403**, 있는 경우 → 정상 수행.
- 화면 이동마다 Backend에 권한 확인 API를 호출한다.
